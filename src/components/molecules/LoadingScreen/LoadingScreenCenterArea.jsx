import { StyledLoadingScreenCenterArea } from './StyledLoadingScreenCenterArea.styled';
import LoadingScreenText from '../../atoms/LoadingScreen/LoadingScreenText';

const LoadingScreenCenterArea = () => {
	return (
		<StyledLoadingScreenCenterArea>
			<LoadingScreenText/>
		</StyledLoadingScreenCenterArea>
	);
};

export default LoadingScreenCenterArea;
